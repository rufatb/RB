# Day100 — evidence coverage and a separate factor watchlist

Registered 2026-09-14 before implementation of the expanded watchlist or any
market-outcome evaluation. This does not reopen the rejected day14 expansion
of the production density selector. All older registrations remain in force.

## Fixed research design

Keep production TSX-21, K=60/M=20, features, sides and allocation unchanged.
Stage a distinct, configured 60-name TSX research roster. The roster is a
coverage target, not a current index membership or liquidity ranking claim.
Verify exact ticker, CAD currency, Toronto exchange and complete prior-session
five-minute data. Compute all indicators with the production Python history
validator and existing metrics. Missing names remain explicit coverage gaps.
No daily bars stand in for intraday VWAP/ORB, and no quantitative score is
invented for a name outside the baseline. Indicators describe completed
sessions, not the current opening range or executable quotes.

DeepSeek receives only validated public evidence. The separate sentiment
watchlist shows at most two BULL and two BEAR assessments with aligned sign
and absolute sentiment >=0.5, descending absolute sentiment then ticker.
NO_EDGE and unassessed/invalid evidence do not qualify. This is a fixed DESIGN
threshold, not a fitted edge, probability, target or entry recommendation.
Missing execution costs cannot hide an otherwise assessed research candidate;
they prevent any claim of execution readiness. Existing day99 H1/H2 remain
unchanged and separate. No quotas or automatic promotion.

## Evidence and evaluation

Preserve dated pre-open inputs, exact model/configuration, hashes, failures and
all assessment outputs. No retry after observing outcomes and no rewrite of
September 14's attempt/publication. A synthetic API contract check is separate
from a market snapshot and says nothing about predictive skill.

The watchlist is unadopted. Minimum 120 complete forward sessions (60
development, then 60 untouched confirmation); retain the four-quarter
robustness requirement before any production expansion. Measure paired net
returns on the union of session opportunities, abstentions as cash, actual
spread/slippage coverage, hit rates, participation and shared exposure. Keep
this third arm in multiplicity accounting with day99 H1/H2. Use five-session
block bootstrap, 2,000 draws, seed 100; report MDE80=(3.5+0.8416212336)*SE.
Without matched forward outcomes/costs, both edge and numerical MDE are
UNAVAILABLE. Do not search this threshold or interpret a successful API call,
broader universe, input correction or passing test as improved accuracy.

## Operational changes

The separate pool stage runs once per session before 09:30, reuses verified
baseline files and stages only additional names, with a 120-second cooperative
budget and killable per-request budgets. Preserve partial checkpoints. Stop
further requests on a rate-limit/authentication/transport outage; do not hammer
the provider. Public evidence and model preparation have independent bounded
budgets; valid independent inputs survive failures. Neither runs at 09:46.

For the explicitly configured Flash model, disable its documented default
thinking mode for this bounded JSON classification task and record that mode.
Do not silently switch models. Legacy/reasoner settings retain their semantics.
Stage Yahoo cookie/crumb authentication near 09:40 in private state with a
15-minute same-session expiry; fetch fresh quote data at publication. Cached
authentication is not a quote, BBO entitlement or timing guarantee.

All six protected modules remain unchanged. One existing Gmail sender consumes
one immutable computation. Tests check contracts, not profitability; final
deployment records must distinguish source changes from actual scheduled pins.
