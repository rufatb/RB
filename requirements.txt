# Core data + math
yfinance>=0.2.40
pandas>=2.0
numpy>=1.24

# Config + terminal output
pyyaml>=6.0
rich>=13.0

# Scheduling + market holidays (optional but recommended)
schedule>=1.2
pandas_market_calendars>=4.0

# Testing
pytest>=7.0

# ── Real-time upgrade adapters (install only the one you use) ───────────────
# ib_insync>=0.9        # IBKRAdapter (Interactive Brokers; needs TWS/Gateway)
# polygon-api-client    # PolygonAdapter
# alpaca-py             # AlpacaAdapter
# Questrade: no official pip client — use its REST API + OAuth refresh token.
